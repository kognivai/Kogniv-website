kind = "web"
previewPath = "/"
title = "Kogniv Website"
version = "1.0.0"
id = "artifacts/kogniv"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23134

[services.development]
run = "pnpm --filter @workspace/kogniv run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kogniv", "run", "build" ]
publicDir = "artifacts/kogniv/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23134"
BASE_PATH = "/"
